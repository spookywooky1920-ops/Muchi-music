# ── Node / wrangler ──
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# ── Local Cloudflare state (never commit) ──
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example

# ── Local environment ──
.env
.env.*
!.env.example

# ── Android / Gradle build output ──
android/.gradle/
android/build/
android/app/build/
android/app/release/
android/local.properties
android/captures/
.gradle/
build/
local.properties
*.apk
*.aab
*.ap_
*.dex
*.class

# ── iOS build output ──
ios/App/DerivedData/
ios/App/output/
*.xcarchive
*.ipa

# ── Signing keystores: never commit ──
*.keystore
*.jks
*.p12
*.pkcs12

# ── IDE / OS ──
.idea/
*.iml
.vscode/
.DS_Store
Thumbs.db
